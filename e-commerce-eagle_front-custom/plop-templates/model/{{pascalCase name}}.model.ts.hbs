export class {{pascalCase name}}Model implements I{{pascalCase name}}Model {
  sample: string;

  constructor(sample: string) {
    this.sample = sample;
  }

  sampleFunction(): void {
    // doesn't do anything, just a sample function
  }
}
