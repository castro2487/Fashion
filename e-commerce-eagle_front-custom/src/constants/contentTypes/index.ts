// Contentful content types IDs
export enum CONTENTFUL_CONTENT_TYPES {
  LINK_ITEM = 'linkItem',
  NAVIGATION = 'navigation',
  BYNDER_IMAGE = 'bynderImage',
  SPOTLIGHT_COMPONENT = 'spotlightComponent',
  TILE_COMPONENT = 'tileComponent',
  RICH_TEXT = 'richText',
  HEADER = 'header',
  CAMPAIGN = 'campaignComponent',
  GRID = 'componentGrid',
}

// Link Item
export enum LINK_ITEM_TYPES {
  CONTENT_PAGE = 'Content Page',
  CATEGORY = 'Category',
  EXTERNAL = 'External',
}

// Navigation categories
export enum NAVIGATION_LIST_TYPES {
  TABS = 'tabs',
  LINKS = 'links',
}
