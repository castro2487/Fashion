'use client';
import { styled } from '@stitches-definitions';

export const Divider = styled('hr', {
  width: '100%',
  height: 1,
  background: 'currentColor',
  margin: '0 auto',
  border: 'none',
});
