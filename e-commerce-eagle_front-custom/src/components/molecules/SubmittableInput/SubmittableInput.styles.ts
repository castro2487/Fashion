import { styled } from '@stitches-definitions';
import { FloatingButton } from '@atoms/FloatingButton/FloatingButton';

export const SubmitButton = styled(FloatingButton, {
  margin: 0,
  boxSizing: 'border-box',
  position: 'initial',
  width: 'auto',
  height: 'auto',
});
