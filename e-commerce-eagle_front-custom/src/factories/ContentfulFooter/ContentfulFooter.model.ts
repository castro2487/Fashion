export class ContentfulFooterModel implements IContentfulFooterModel {
  sample: string;

  constructor(sample: string) {
    this.sample = sample;
  }

  sampleFunction(): void {
    // doesn't do anything, just a sample function
  }
}
